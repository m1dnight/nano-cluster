%%
%% Vendored from atomvm_lib (include/httpd.hrl @ master), unchanged. HTTP status
%% codes used by httpd.erl.
%%

-define(INTERNAL_SERVER_ERROR, 500).
-define(BAD_REQUEST, 400).
-define(NOT_FOUND, 404).
-define(OK, 200).
-define(CONTINUE, 100).
-define(SWITCHING_PROTOCOLS, 101).
